1-monitor.bat: deactivate all but the center screen.
2-monitors.bat: activate the center and right screens.
3-monitors.bat: activate all three screens.
-> require MultiMonitorTool-x64


autohotkey.ahk: switches è and backquote


backup.bat: performs a simple backup (only the source directory of every code system)
full_backup.bat: performs a full backup (simple + all specific directories)
-> require backup files + idrive api


close-all-windows.bat: Close (not kill: applications will thus ask to save work in progress) all windows.
-> Require CMDOW


set-up-windows.bat: set up windows environment for the first time


shutdown.bat: closes all windows and performs a simple backup before shutting down


start-dev-environment.vbs: Start Lisp development session